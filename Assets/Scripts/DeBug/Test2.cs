using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test2 : MonoBehaviour
{
   /* void Start()
    {
        GameEvent.Instance.Register(ListEvent.OnClick.ToString(), (Action<int>)OnTakeDamage);
    }

    // Update is called once per frame
    void Update()
    {

    }
    void OnTakeDamage(int damage)
    {
        Debug.Log($"[Listener] Player2 takes {damage} damage!");
    }*/
}
